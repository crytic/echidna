{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Target Tool Response",
  "description": "Target contract name and ABI",
  "type": "object",
  "properties": {
    "response": {
      "type": "string",
      "description": "Contract name and list of function signatures",
      "example": "Contract: MyToken\nFunctions:\n- approve(address,uint256)\n- balanceOf(address)\n- transfer(address,uint256)\n- transferFrom(address,address,uint256)"
    }
  },
  "required": ["response"]
}
