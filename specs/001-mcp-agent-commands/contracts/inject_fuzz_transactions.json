{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Inject Fuzz Transactions Tool",
  "description": "Inject transaction sequence for fuzzing with optional concrete arguments",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "transactions": {
          "type": "string",
          "description": "Semicolon-separated transaction sequence. Use '?' for fuzzer-generated values.",
          "examples": [
            "transfer(0x123, 100)",
            "approve(?, 1000); transferFrom(0xABC, 0xDEF, ?)",
            "deposit(); withdraw(?)"
          ]
        }
      },
      "required": ["transactions"]
    },
    "response": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "Success message or error description",
          "examples": [
            "Requested fuzzing of transaction sequence 'transfer(0x123, 100)' on 4 fuzzers",
            "Error: Function 'invalidFunc' not found.",
            "Error:\nFunction 'transfer' found but with different argument count. Expected: [2], Got: 1"
          ]
        }
      },
      "required": ["message"]
    }
  },
  "required": ["input", "response"]
}
