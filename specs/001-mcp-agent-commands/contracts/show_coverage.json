{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Show Coverage Tool Response",
  "description": "Detailed coverage report for specific contract",
  "type": "object",
  "properties": {
    "contract": {
      "type": "string",
      "description": "Contract name"
    },
    "response": {
      "type": "string",
      "description": "Source code with coverage annotations showing covered lines, uncovered lines, and execution counts",
      "example": "path/to/Contract.sol:\n  25 | *     function transfer(address to, uint256 amount) {\n  26 | *         balances[msg.sender] -= amount;\n  27 |           balances[to] += amount;\n..."
    }
  },
  "required": ["response"]
}
