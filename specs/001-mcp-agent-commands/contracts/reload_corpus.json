{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Reload Corpus Tool Response",
  "description": "Result of corpus reload operation",
  "type": "object",
  "properties": {
    "response": {
      "type": "string",
      "description": "Status message indicating number of reloaded sequences or no new sequences found",
      "examples": [
        "Reloaded 15 new transaction sequences from /path/to/corpus",
        "No NEW transaction sequences found in corpus directory.",
        "No transaction sequences found in corpus directory."
      ]
    }
  },
  "required": ["response"]
}
